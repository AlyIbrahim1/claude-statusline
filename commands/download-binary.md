Download the native Rust binary for claude-statusline.

Plugin installs do not run `npm install`, so the pre-compiled Rust binary is not downloaded automatically. Use this command to install it on demand. The binary enables the terminal TUI history dashboard and renders the statusline faster than the JS fallback.

Run in your terminal:

claude-statusline download-binary
claude-statusline setup
