{
  "hooks": {
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${STATUSLINE_CMD} hook end --marker=${HOOK_MARKER}"
          }
        ]
      }
    ]
  }
}
